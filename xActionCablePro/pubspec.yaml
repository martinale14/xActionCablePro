name: x_action_cable_pro
description: "A new Flutter package project."
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.12.0
  x_action_cable:
    git:
      url: https://github.com/martinale14/xActionCable
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: